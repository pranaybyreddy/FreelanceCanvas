<%=  bootstrap_form_for @comment, url: "/posts/#{comment[:post_id]}/comments", method: "post" do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= form.text_area :body, content:nil, placeholder: "Leave a comment..."  %>

  </div>

  <%= form.hidden_field :post_id %>
  <div class="actions">
    <%= form.submit "Submit Comment", class: "btn btn-success custom" %> <!--should process show.html.erb -->
  </div>
<% end %>
